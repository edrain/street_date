<div class="row">
  <ol class="instructions_ol">
	  <% if not user_signed_in? %>		
	  	<li>
		  <%= link_to('Sign in', new_user_session_path) %> or <%= link_to "sign up", new_user_registration_path %>
		</li>
	  <% end %>
	  <li>Search for artists</li>
	  <li>Make a 
	    <% if user_signed_in? %> 
	    <%= link_to "list", list_items_path(:id => current_user.id) %>
		<% else %>
		list
	 	<% end %>
	  </li>
	  <li>Get an email when your artists release new content</li>
	</ol>
</div>
